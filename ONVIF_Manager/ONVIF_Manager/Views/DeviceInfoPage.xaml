<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="ONVIF_Manager.Views.DeviceInfoPage"
             Appearing="OnAppearing">
    <ContentPage.Content>
        <StackLayout Spacing="5" Padding="5">
            <Label Text="Manufacturer" FontSize="Small" />
            <Entry Text="{Binding Manufacturer}" d:Text="Manufacturer" FontSize="Small" IsEnabled="false" />
            <Label Text="Model" FontSize="Small" />
            <Entry Text="{Binding Model}" d:Text="Model" FontSize="Small" Margin="0" IsEnabled="false" />
            <Label Text="FirmwareVersion" FontSize="Small" />
            <Entry Text="{Binding FirmwareVersion}" d:Text="FirmwareVersion" FontSize="Small" Margin="0" IsEnabled="false" />
            <Label Text="SerialNumber" FontSize="Small" />
            <Entry Text="{Binding SerialNumber}" d:Text="SerialNumber" FontSize="Small" Margin="0" IsEnabled="false" />
            <Label Text="HardwareId" FontSize="Small" />
            <Entry Text="{Binding HardwareId}" d:Text="HardwareId" FontSize="Small" Margin="0" IsEnabled="false" />

        </StackLayout>
    </ContentPage.Content>
</ContentPage>